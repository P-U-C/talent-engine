{
  "key": "sponsorship-application",
  "title": "Builder Sponsorship — application",
  "description": "Question labels are load-bearing. The webhook parser identifies fields by matching words in the label against the alias tables in talent_engine/ingest/normalize.py, so renaming a question can silently stop a field being read. tests/test_form_spec.py round-trips this file through the parser and fails if any mapped question stops resolving. Change labels here, not in Tally's editor.",
  "questions": [
    {
      "label": "Your name",
      "type": "INPUT_TEXT",
      "required": true,
      "maps_to": "contact.name",
      "why": "Quarantined. Never enters an assessment record."
    },
    {
      "label": "Email",
      "type": "INPUT_EMAIL",
      "required": true,
      "maps_to": "contact.email",
      "why": "Quarantined. How you tell someone they were selected."
    },
    {
      "label": "Telegram handle",
      "type": "INPUT_TEXT",
      "required": false,
      "maps_to": "contact.telegram",
      "why": "Quarantined. Matches HANDLE_KEYS on the word 'handle', which is why the parser skips contact-labelled fields when looking for the GitHub login."
    },
    {
      "label": "X (Twitter) handle",
      "type": "INPUT_TEXT",
      "required": false,
      "maps_to": "contact.x",
      "why": "Quarantined."
    },
    {
      "label": "GitHub username",
      "type": "INPUT_TEXT",
      "required": true,
      "maps_to": "handle",
      "why": "The only field the automated score depends on. A response without a usable one is stored for human review, never zero-scored."
    },
    {
      "label": "Which repo should we look at first?",
      "type": "INPUT_TEXT",
      "required": true,
      "maps_to": "application.declared_repo",
      "why": "Monitored during the program to see whether the funded work actually moves."
    },
    {
      "label": "Build plan — what will you make with this sponsorship?",
      "type": "TEXTAREA",
      "required": true,
      "maps_to": "application.build_plan",
      "why": "Collected and shown to reviewers, deliberately unscored. The overlay requires it for selection; scoring free text would make the rubric unreproducible."
    },
    {
      "label": "How does this connect to Celo?",
      "type": "TEXTAREA",
      "required": true,
      "maps_to": "application.extra",
      "why": "Program fit, assessed by a human at selection and again at the month-two gate. Not a scored dimension — fit is a program question, not an evidence question."
    },
    {
      "label": "Referred by",
      "type": "INPUT_TEXT",
      "required": false,
      "maps_to": "application.referrer_name",
      "why": "Scores only on exact match against the program's registry. An unrecognised name scores zero and flags — so leave it blank rather than guessing."
    },
    {
      "label": "Which of these factors apply to you?",
      "type": "CHECKBOX",
      "required": false,
      "maps_to": "application.context_factors",
      "options": [
        "Self-taught, no CS degree",
        "No institutional or investor funding",
        "No local technology industry where I live",
        "Building alongside unrelated paid work",
        "None of these"
      ],
      "why": "Self-declared only. Invariant 2: nothing about a person's circumstances is ever inferred. Tally sends option ids, not text — the parser resolves them, or these would score as unrecognised factors."
    },
    {
      "label": "Anything about your context we should know?",
      "type": "TEXTAREA",
      "required": false,
      "maps_to": "application.context_statement",
      "why": "Capped by the rubric weight and disabled entirely for recruitment-mode programs. Self-declared and unverifiable by design."
    }
  ]
}
